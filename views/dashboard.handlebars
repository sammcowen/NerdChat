<div>
    <a href="/dashboard/create">Create New Post </a>
    <h2>Dashboard: Your Posts </h2><hr>
    {{#if posts.length}}
</div>
{{#each posts as |post|}}
    {{>post-info post}}

    <a href="/dashboard/edit/{{post.id}}"><button>Edit Post </button></a>
    </div>
    {{/each}}

    {{else}}    
    <h6>You do not have any posts</h6>
    {{/if}}